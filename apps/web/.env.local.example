# Copy this to .env.local and fill in values

# Anthropic API key — used by /api/chat in Phase 1
# Get one at https://console.anthropic.com
ANTHROPIC_API_KEY=

# FastAPI backend URL — used in Phase 2+ when /api/chat becomes a proxy
API_URL=http://localhost:8000
